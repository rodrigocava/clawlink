# ClawPulse Configuration
# Copy this file to .env and edit as needed
# docker compose will load .env automatically

# Port to expose the server on
PORT=6413

# Host path where the SQLite database is stored
# Defaults to ./data (relative to this file)
# Example for a custom data disk:
#   DATA_DIR=/srv/your-data-disk/clawpulse
DATA_DIR=./data

# Hours before stored payloads auto-expire (default: 48)
DATA_TTL_HOURS=48

# Max upload size in bytes (default: 10MB)
MAX_PAYLOAD_BYTES=10485760

# Max payload size per token in bytes (default: 5MB)
# Prevents a single token from storing excessive data
MAX_TOKEN_QUOTA_BYTES=5242880

# Shared secret that all ClawPulse clients must send as X-ClawPulse-Secret header
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# Leave empty to disable (dev/self-host mode — NOT recommended for public servers)
CLIENT_SECRET=
